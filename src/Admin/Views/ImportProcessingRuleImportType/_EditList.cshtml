@model Admin.Models.ImportProcessingRuleImportType.ListViewModel

@if (Model.Items != null && Model.Items.Any())
{
    <div class="table-responsive">
        <table class="table table-striped table-bordered table-hover mt-2">
            <thead>
                <tr>
                    <th scope="col">
                        Name
                    </th>
                    <th scope="col">
                        Description
                    </th>
                    <th scope="col">

                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Items)
                {
                    <tr>
                        <td>
                            @item.ImportType.Name
                        </td>
                        <td>
                            @item.ImportType.Description
                        </td>
                        <td style="width:20%;" class="text-end">
                            @Html.ActionLink("Edit", "Edit", "ImportProcessingRuleImportType", new { id = item.Id }, new { @class = "btn btn-secondary" })
                            @Html.ActionLink("Delete", "Delete", "ImportProcessingRuleImportType", new { id = item.Id, importProcessingRuleId = item.ImportProcessingRuleId }, new { @class = "btn btn-secondary delete-import-type-import-processing-rule" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}
else
{
    <div class="ui info message">
        There are no linked import types
    </div>
}

<script type="text/javascript">
    $(document).ready(function () {

        $(".delete-import-type-import-processing-rule").on("click",
            function (e) {
                if (window.confirm("Are you sure you want to delete this link?")) {

                } else {
                    e.preventDefault();
                    e.stopPropogation();
                    return false;
                }
            });

    });
</script>
